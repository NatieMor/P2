<br>
<h2 class="text-center">Inscribirse en nuestra pagina</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field mb-3">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field mb-3">
    <%= f.label :Contraseña %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> carracteres minimo)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "nueva contraseña" %>
  </div>

  <div class="field mb-3">
    <%= f.label :Confirma_contraseña %><br />
    <%= f.password_field :password_confirmation, autocomplete: "nueva contraseña" %>
  </div>

  <div class="actions">
    <%= f.submit "Enviar" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
